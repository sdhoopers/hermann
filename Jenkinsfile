#!groovy

pipeline {
    agent {
        dockerfile true
    }
    stages {
        stage ('Build') {
            // agent {
            //     dockerfile true
            // }
            steps {
                // checkout scm
                sh 'cat Jenkinsfile'
            }
        }
    }
}
